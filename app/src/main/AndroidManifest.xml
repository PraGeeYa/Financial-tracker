<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.coinomy">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/_11"
        android:label="@string/app_name"
        android:roundIcon="@drawable/_11"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoinomyMADlabexam3"
        tools:targetApi="31">
        <activity
            android:name=".CurrencyFormatter"
            android:exported="false" />
        <!-- LoginActivity as launcher -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- RegisterActivity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <!-- MainActivity (no longer launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".PasscodeActivity" />
    </application>

</manifest>